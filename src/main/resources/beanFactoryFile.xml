<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd"
>
    <!-- bean元素：使用该元素描述需要spring容器管理的对象 -->
    <!--
        name:调用时用的名字
        class：路径
        id：与name属性作用一样但名称不可重复且不能使用特殊字符
        scope属性：
            singleton（默认值）：单例对象，在spring容器中启动时创建，只存在一个
            prototype：多例原型，每次在获得时才会创建，每次都创建一个新的
    -->

    <bean id="myBeanPostProcessor" class="com.common.util.spring.MyBeanPostProcessor"></bean>

    <bean id="user" class="com.common.util.spring.User">
        <property name="name" value="周成功"></property>
        <property name="age"  value="23"></property>
    </bean>

    <bean id="hellomessage" class="com.common.util.spring.HelloMessage">
        <property name="message" value="${helloMessage.message}"></property>
        <property name="time" value="${helloMessage.time}"></property>
    </bean>

    <!--PropertyPlaceholderConfigurer 使用方式  -->
    <bean id="mesHandler" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>bean.properties</value>
            </list>
        </property>
    </bean>


    <bean id="myBeanFactoryPostProcessor" class="com.common.util.spring.MyBeanFactoryPostProcessor">
        <property name="filterStrings">
            <set>
                <value>hello</value>
                <value>World</value>
            </set>
        </property>
    </bean>

    <!-- xml配置BeanDefinitionRegistryPostProcessor -->
    <bean id="customBeanDefinitionRegistry" class="com.common.util.spring.CustomBeanDefinitionRegistry">

    </bean>

    <bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
        <property name="basenames">
            <list>
                <!--资源文件地址-->
                <value>i18n.message.messages</value>
            </list>
        </property>
    </bean>

    <!-- ApplicationListener的使用 -->
    <bean id="applicationTestListener" class="com.common.util.spring.ApplicationTestListener">

    </bean>

</beans>